cores_list = ['amarelo', 'vermelho', 'azul']
cores_tuple = ('amarelo', 'vermelho', 'azul')

print(type(cores_list))
print(type(cores_tuple))

# quando usar lista que for mudada, use listas
# quando usar lista que não for mudada, use tuple
